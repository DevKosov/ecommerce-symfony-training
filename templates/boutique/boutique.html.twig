{% extends "base.html.twig" %}

{% block body %}
    <div class="card col-md-12">
        <div class="card-header">
            <h1><i class="fas fa-store"></i> {{ 'default.boutique' | trans }} : 4 {{ 'default.boutique.rayons' | trans }}</h1>
        </div>
        <div class="card-body row">

            {% for category in categories %}
                {% include "boutique/category.html.twig" %}
            {% endfor %}

        </div>
    </div>
{% endblock %}